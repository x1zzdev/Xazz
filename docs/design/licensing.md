# Licensing Policy

- Status: **Marker** (상업 라이선스 표식)
- 관련 문서: [`LICENSE`](../../LICENSE) (Apache-2.0), `README.md`

## 의도

Xazz의 소스는 Apache-2.0이다. 제품화 시 상용 배포에 부여하는
사용권은 **계약의 증거로 서는 코드키**로 나타내며, 이 키의 강제는
**법적**으로 한다. 아래 표식 선택(옵션 1)은 오픈소스 소스와 양립하며
구현이 작다.

## 의미

- 키는 "구매자와 공급자 간 사용권 계약이 존재함"의 **증거 표식**이다.
- **기술적 잠금이 아니다.** 검사 코드를 지우고 재빌드하면 우회되는
  로컬 잠금을 의도하지 않는다(옵션 3은 채택하지 않음).

## 경계 (state-contract §7 준수)

- 구현되지 않은 능력을 구현된 것처럼 표시하지 않는다.
- "잠겨 있다"는 잘못된 인상만 남기는 로컬 DRM 배지를 다지 않는다.
- 기술적으로 강제할 수 없는 상태는 `licensed` 배지로 표기하지 않는다.
  상업 권리는 계약(법적)으로, 소스 오픈은 Apache-2.0으로 별도 표기한다.
- 향후 중앙 호스팅 검증(옵션 2)은 호스팅 계획이 설 때 별도 문서에서
  정의한다. 그 전까지 서버 쪽 검증은 구현하지 않는다.

## 배포

현재 릴리스는 코드키를 요구하지 않는다. 키 기반 상용 채널을 열면:
키를 계약 증거로 보관하고, 그 항목만이 이 섹션의 "상위 키"에 해당한다.