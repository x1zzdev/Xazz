/target

# IDE
.idea/

# Xazz — Rule files (security: keep out of version control)
.roorules
.clinerules
.roo/

xazz-sde

benches/data/*.csv
__pycache__/
*.pyc

# IDE data folder (runtime uploads from xazz-server)
uploads/

# Environment / local secrets
.env

# Windows metadata artifacts
*.Zone.Identifier

# Personal development notes (not for distribution)
commitGuide.txt

# Temporary backup and build artifacts
core_code.zip
final_result_chart.html
result_file_input_chart.html
dist/

# Personal documents
@Xazz_MVP_Roadmap_v3.docx
# xazz runtime outputs
audit_log/
checkpoints/
*_chart.html

# 보안 데모용 합성 데이터 (생성기로 재생성 — examples/security/generate_patients.py)
examples/security/data/

# 시연 영상 생성물 (demo/capture.sh + demo/make_video.py 로 재생성)
demo/_capture/
demo/_build/
demo/_shots/
demo/_voice/
demo/*.mp4
demo/*.wav
demo/*.srt
demo/narration.txt

# 다만 제출용 최종본 한 벌은 커밋한다 — 툴체인 없이도 바로 볼 수 있도록.
# 다시 렌더할 때마다 커밋하지 말 것 (1MB 바이너리가 이력에 계속 쌓인다).
!demo/xazz_demo.mp4
!demo/xazz_demo.srt
